import { SliderPickerDirective } from './slider-picker.directive';

describe('SliderPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderPickerDirective();
    expect(directive).toBeTruthy();
  });
});
